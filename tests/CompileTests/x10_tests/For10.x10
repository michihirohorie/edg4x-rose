public class For10 {
    val N = 100n;
    val Hoge = 0n..(N-1n);;
    val P = 2n;
	public def loop() {
/*
        var range : Rail[IntRange] = R.split(P);
        for (var i: Long = 0; i < P; ++i) {
           var ra : IntRange = range(i);
           for (j in ra) {}
        }
*/
       for (i in Hoge.split(P)) {}
	}
}
